# Slice 2026 Archive

This folder keeps the historical generated artifacts from the earlier slice
results tree.

The runnable slice backend now lives in `unified/obj2anthro/backends/slice/`.
Core OBJ inputs live in `data/obj/`.
