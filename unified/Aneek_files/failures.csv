"""Rebuild the avatar_conversion package folder from loose .py files.

Run this once if you downloaded the individual files instead of the zip and hit
``ModuleNotFoundError: No module named 'avatar_conversion'``.
"""

import shutil
from pathlib import Path

HERE = Path(__file__).resolve().parent
PKG = HERE / "avatar_conversion"
MODULES = ["mesh_io.py", "matlab_ops.py", "matlab_avatar.py"]

INIT = '''"""MATLAB-faithful Python port of Avatar.m."""

from .matlab_avatar import MatlabAvatar
from .mesh_io import load_obj

__all__ = ["MatlabAvatar", "load_obj"]
__version__ = "1.0.0"
'''

PKG.mkdir(exist_ok=True)

moved, already, missing = [], [], []
for name in MODULES:
    target = PKG / name
    loose = HERE / name
    if target.exists():
        already.append(name)
    elif loose.exists():
        shutil.move(str(loose), str(target))
        moved.append(name)
    else:
        missing.append(name)

(PKG / "__init__.py").write_text(INIT)

print(f"package folder: {PKG}")
if moved:
    print(f"  moved into place: {', '.join(moved)}")
if already:
    print(f"  already correct:  {', '.join(already)}")
print("  wrote __init__.py")

if missing:
    print(f"\nSTILL MISSING: {', '.join(missing)}")
    print("Download these from the chat and put them next to this script, "
          "then run it again.")
else:
    print("\nStructure is correct. Now run:")
    print("  python selftest.py /path/to/your.obj")
