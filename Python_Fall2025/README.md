# Relocated

The project formerly stored at `Python_Fall2025/` was relocated verbatim to
`unified/obj2anthro/backends/segmentation/`.

Its original README is now:

`unified/obj2anthro/backends/segmentation/README.md`

Old native usage:

`cd Python_Fall2025 && python -m src.main ...`

New native usage:

`cd unified/obj2anthro/backends/segmentation && python -m src.main ...`

New unified usage:

`python -m unified obj2anthro --input ... --method segmentation`

Do not add new implementation files here. Update the relocated project instead.
See `unified/RELOCATION_MAP.md` for the complete mapping.
